{
  "framework": "vite",
  "installCommand": "if [ -d frontend ]; then npm install && cd frontend && npm install; else npm install; fi",
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
